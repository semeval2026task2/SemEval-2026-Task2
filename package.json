{
  "devDependencies": {
    "concurrently": "^9.2.0",
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "dev": "concurrently \"npm --prefix documentation-ui run dev\" \"npm --prefix server run dev\"",
    "build-docs": "npm --prefix documentation-ui run build",
    "deploy-docs": "npm --prefix documentation-ui run deploy"
  },
  "dependencies": {
    "remark-footnotes": "^4.0.1",
    "remark-gfm": "^4.0.1",
    "xlsx": "^0.18.5"
  }
}
